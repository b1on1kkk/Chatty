"use client";
export default function Home() {
  return (
    <div>
      get here value from server if user logged in. if logged in - route him to
      the chat page parsing his friends. otherwize if user not logged in - route
      to logged in/sign up page.
    </div>
  );
}
